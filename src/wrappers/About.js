import styled from "styled-components";

const Wrapper = styled.section`
  .img {
    height: 500px;
    width: auto;
  }
  .center {
    display: block;
    margin-left: auto;
    margin-right: auto;
  }
`;

export default Wrapper;
